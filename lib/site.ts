// Single source of truth for the site's public URL.
// TODO: swap to the real domain once connected — this one constant updates the
// canonical URL, sitemap, robots, OG/Twitter image URLs, and all structured data.
export const SITE_URL = "https://robadeli.fi";
